<?xml version="1.0"?>

<project name="mdr" default="archive" basedir=".">
    <description>
           Struts 2 Wildcard Method Selection Java Web Application
    </description>
	<property file="build.properties"/>

	<path id="junit.classpath">
		<fileset dir="${lib.home}">
			<include name="junit*.jar"/>
		</fileset>
		<fileset dir="${deploy.home}/lib" erroronmissingdir="true">
			<include name="junit*.jar"/>
		</fileset>
	</path>

	<taskdef name="junit" classname="org.apache.tools.ant.taskdefs.optional.junit.JUnitTask"
		classpathref="junit.classpath"/>



	<!-- ==================== Clean Target ==================================== -->
	<target name="clean" description="Delete old build and dist directories">
		<delete dir="${dist.home}"/>
		<delete dir="${build.home}"/>
	</target>

	<!-- ==================== Init Target ================================== -->
	<target name="init" depends="clean"  description="Create build directory">
		
		<mkdir dir="${build.home}" />

	</target>
	
	<!-- ==================== Compile Target ================================== -->
	<target name="compile" depends="init" description="Compile Java sources">
		<mkdir dir="${build.home}/WEB-INF/classes" />
		<javac srcdir="${source.home}"
				destdir="${build.home}/WEB-INF/classes"
				debug="${compile.debug}"
				deprecation="${compile.deprecation}"
				optimize="${compile.optimize}"
			     source="1.8" target="1.8">
			
			<classpath>
				<path>
			    	<fileset dir="${lib.home}" />
                    <fileset dir="${deploy.home}/lib" />
			    </path>
			</classpath>
			
		</javac>

	</target>
	
	<!-- ==================== Build Target ================================== -->
	<target name="build" depends="compile" description="Copies all non Java classes to build directoy">
		<copy todir="${build.home}">
			<fileset dir="${webapp.home}" excludes="CVS,**/*.class" />
		</copy>
		<copy todir="${build.home}/WEB-INF/classes">
			<fileset dir="${resources.home}" excludes="CVS,**/*.class" />
		</copy>


		<copy todir="${build.home}/WEB-INF/classes">
			<fileset dir="${source.home}" excludes="CVS,**/*.java" />
		</copy>
	</target>

	<!-- ==================== Test Target ================================== -->
	<target name="test" depends="build" description="Compile and run unit tests">
		<mkdir dir="${build.home}/test-classes" />
		<mkdir dir="${build.home}/test-reports" />
		<javac srcdir="${test.source.home}"
			destdir="${build.home}/test-classes"
			debug="${compile.debug}"
			deprecation="${compile.deprecation}"
			optimize="${compile.optimize}"
			source="1.8"
			target="1.8"
			includeantruntime="false">
			<classpath>
				<pathelement path="${build.home}/WEB-INF/classes" />
				<fileset dir="${lib.home}" />
				<fileset dir="${deploy.home}/lib" />
			</classpath>
		</javac>

		<junit printsummary="on" haltonfailure="true" fork="true">
			<classpath>
				<pathelement path="${build.home}/test-classes" />
				<pathelement path="${build.home}/WEB-INF/classes" />
				<fileset dir="${lib.home}" />
				<fileset dir="${deploy.home}/lib" />
			</classpath>
			<formatter type="plain" />
			<batchtest todir="${build.home}/test-reports">
				<fileset dir="${test.source.home}">
					<include name="**/*Test.java" />
				</fileset>
			</batchtest>
		</junit>
	</target>
	
	<!-- ==================== Archive Target ================================== -->
	<target name="archive" depends="build" description="Create binary archive of all files in dist.home">
		<mkdir     dir="${dist.home}" />
		<!-- Create application WAR file -->
	    <jar jarfile="${dist.home}/${app.name}.war"
				basedir="${build.home}" />
	</target>

    <target name="deploy" depends="archive" description="Deploying build war file">      
      <copy todir="${deploy.home}/webapps/">
            <fileset dir="${dist.home}">
                  <include name="${app.name}.war"/>
            </fileset>
      </copy>

     </target>
      


	
</project>

